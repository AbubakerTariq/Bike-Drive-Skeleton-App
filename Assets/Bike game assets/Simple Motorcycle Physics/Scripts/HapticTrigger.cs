using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HapticTrigger : MonoBehaviour
{
    [SerializeField] private Transform targetWaypoint;
    [SerializeField] private string hapticType;
    [SerializeField] private float hapticFactor;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
